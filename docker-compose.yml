services:
  app:
    build: .
    container_name: farm-helper
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/farm.db
    restart: unless-stopped
